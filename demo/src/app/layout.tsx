import type { Metadata } from 'next';
import type { ReactNode } from 'react';
import { Providers } from './providers';
import './globals.css';

export const metadata: Metadata = {
  title: 'USL demo: one signature, limited session',
  description: 'One explicit mandate across Ethereum, Base and Arbitrum Sepolia. Browser-held session keys, contract-enforced limits, and direct wallet submission.',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
